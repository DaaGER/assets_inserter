name: assets_inserter
description: "AssetsInserter automates adding asset paths to Flutter code with using templates."
version: 0.0.3
homepage: https://github.com/DaaGER/assets_inserter

environment:
  sdk: '>=3.2.6 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
